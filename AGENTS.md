# 本地工作目录管理

用户要求：同一 disclaude 仓库统一使用 Git worktree，避免临时 clone、fork 副本和重复依赖长期占用磁盘。

- 从现有主仓库使用 `git worktree add` 创建任务目录；不为同一仓库重新 clone 或复制完整目录。
- 只保留当前仍在开发或验证的 worktree；任务结束后检查未提交内容和未推送提交，保留必要提交引用及改动，再用 `git worktree remove` 清理，随后 `git worktree prune`。
- 按需安装依赖，复用包管理器下载缓存；不要跨 worktree 链接整个 workspace 的 node_modules，以免依赖指向另一分支源码。临时安装目录、构建产物和验证副本用完即清理。
- 不清理主仓库的用户数据、运行配置或正在运行的服务目录。无法判断归属的未提交工作先保留。

## 0.6.0 推进期间的 GitHub 反馈检查

- 每次开始工作时读取 hs3180/disclaude 的 0.6.0 issues、相关 PR 的 reviews/行评论、讨论与 CI。持续会话内以 15 分钟为间隔，在阶段完成或等待 CI 等空闲点检查新增反馈；开发优先，不为轮询中断正在执行的操作。
- 先处理评审阻塞和实际用例失败，再推进交付目标；区分真实产品验收、实验结果和仍未验证的条件。Agentic Research 是持久项目及工作空间 UX，不能用 Skill Workflow 代替验收。
- 将检查时间、重要反馈和下一步保存在主仓库 `.local/060-feedback.md`。没有新反馈时不重复发布 GitHub 评论；只有检查到新证据才更新交付状态。
- 此规则是持续会话中的工作约定，不代表已经创建会话外的后台定时任务。

## 文档同步

- 每次发现、修复、设计决策、范围变化或真实验收结果，都要在同一个工作阶段同步写入 `docs/` 下对应的当前文档；PR 描述和 `.local/060-feedback.md` 不能替代仓库文档。
- `docs/` 只保留当前设计契约、实现边界、操作指南和可复用的测试边界。历史过程、一次性候选输出、已取代方案和重复日志放在 Git 历史、Issue 或 PR 中，不新增历史档案。
- Research 必须复用现有 Project 约束 workspace；不要为 Research 创建独立 workspace、任务数据库、固定阶段、卡片仪表盘或其他平行设计。

## 用户更新的持续任务边界（2026-09-16）

- 禁止自行合并任何 PR。只能提出或修改 PR；合并由用户决定并执行。不得运行 PR merge、启用 auto-merge、加入 merge queue，或通过直接推送目标分支等方式绕过此限制。CI 通过或评审通过也不构成合并授权。此规则覆盖此前持续推进 0.6.0 的合并约定。
- 保持日常 disclaude 飞书常驻服务运行；暂缓真实飞书交互验收。不停止、重启或替换该服务，不为验收建立竞争的机器人 WebSocket 连接。
- 继续可独立进行的开发、PR 修改及反馈检查。真实交互验收标记为用户暂缓，不能标记为通过，也不能据此宣称 0.6.0 已交付。

## 后续用户授权更新（2026-09-16 真实验收）

用户随后要求“进行真实飞书交互验收”“直接用生产机器人测试”，并已打开飞书。此前暂缓验收及必须独立机器人限制已被后续授权取代。允许在保留配置和 workspace、避免双连接的前提下，为验收短暂切换生产机器人到候选版本；切换前检查在途任务，验收后恢复原服务并核验健康。禁止自行合并 PR 的约束继续有效。不要因旧暂缓条款重复请求授权。

## 后续用户授权更新（2026-09-17 生产环境验收）

用户明确允许“打断生产机器人服务直接在生产环境测试飞书相关的新功能”。不再因日常机器人在途任务而暂缓候选切换。仍须保留原配置和 workspace、避免竞争连接、记录实际中断和候选来源，并在验收结束后恢复原服务及验证健康。禁止自行合并 PR 继续有效。

## 用户模型要求（2026-09-18）

- 所有 disclaude 日常运行、候选启动、真实模型测试和协议探针均使用 `gpt-5.6-luna`，禁止使用 Astra。
- 启动前核对实际加载配置中的默认模型、Codex 预设及命令行覆盖；不得依赖可能指向其他模型的全局默认值。

## 用户更新：Computer use 验收成本（2026-09-18）

- 飞书 feature 验收须控制 computer use 调用次数与多模态 token；每轮先设少量 UI 操作预算。
- API、日志、协议记录和文件可核验的结果优先走这些途径；仅必要原生交互或视觉状态检查使用 UI，避免重复截图、完整无障碍树和盲目重试。
