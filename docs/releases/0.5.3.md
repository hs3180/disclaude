# disclaude 0.5.3 release scope

Status: in progress, reviewed on 2026-09-13. No 0.5.3 candidate or release has been published. The evidence below separates implemented work, open PRs, and outstanding acceptance gates.

## Evidence and release position

The 0.5.2 P0 scope is closed. Main now includes persistent-runtime-error handling
(#4989 via #4994, commit `6ac98395`). The skill-resolution work remains an open
stack, #4995 → #4996 → #4997 → #4998. Review on #4995 requires a registry shared
by all harnesses; the updated stack exposes it from `@disclaude/core`, under
`src/skills`, with Codex source configuration confined to a consumer adapter.
This shared foundation does not by itself complete Claude/Pi/DSH migration.

#4993 and #4991 remain separate plugin and shared-resource programs; they are
not hidden dependencies of this patch release.

## Version position

**Deterministic skill resolution and truthful terminal delivery.**

Skills are resolved once through a verifiable trust boundary and consumed consistently by each harness. Runtime failures, task completion, and command schedules provide correct terminal behavior rather than misleading recovery or extra chat noise.

## P0 release goals

### 1. One trusted Skill resolution path

Deliver a harness-neutral registry, exported by `@disclaude/core`, as the source of truth for discovery, precedence, metadata validation, filesystem trust, and diagnostics. Connect Codex as its first consumer without losing project-local skills; keep source-layout and transport configuration outside the shared core.

- #4908: unified `SkillsRegistry` core
- #4910: trust-boundary and hostile/malformed-resource hardening
- #4909: one resolved manifest for Codex exec and app-server
- #4992: resolve project root separately from transient chat workspace

Acceptance:

- Codex exec and app-server expose the same resolved manifest for a given project/configuration.
- A project-local skill remains visible when the state workspace is elsewhere; model-facing manifest references are safe and never rely on an absolute host path.
- Malformed frontmatter, unsafe paths, and untrusted resources fail closed with actionable diagnostics; they cannot escape configured roots or silently alter the manifest.
- Codex regressions cover project/user/builtin precedence, root separation, transport parity, and invalid resources.

### 2. Truthful terminal delivery and managed command execution

Make task completion, error recovery, and scheduled child commands match the state the user sees.

- #4989: persistent runtime errors do not enter reconnect/backoff or consume retry budget
- #4984: command schedules inherit the live managed REST endpoint and authentication context
- #4907: use a completion reaction on the actual final delivery instead of a standalone `✅ Complete` message

Acceptance:

- Non-transient errors yield one actionable terminal result and do not display a reconnect notice or alter reconnect accounting; transient failures retain bounded recovery.
- A command schedule can invoke the packaged channel CLI after a dynamically bound service start, in both authenticated and unauthenticated modes, without hard-coded ports or leaked credentials.
- A successful turn has exactly one final user-visible delivery, with its completion state attached to that delivery; cancellation, delivery failure, and no-final-output cases have explicit behavior.

## P1: desirable but non-blocking

- #4911 and #4912: migrate Claude/Pi/DSH and add `skills doctor` after Codex registry behavior is proven.
- #4929: schedule-mode guidance.
- #4900: harness-specific declarations for interactive tools.
- #4892 and #4893: capability-aware welcome and next-step guidance.
- #4990: static Feishu Card JSON 2.0 delivery.

These improve discoverability once the registry is stable, but must not delay a release that makes skill resolution and terminal delivery reliable.

## Explicitly deferred

| Area | Issues | Reason |
| --- | --- | --- |
| Harness terminology, pluginization, and chat runtime-profile UI | #4904, #4905, #4913, #4914 | A breaking architecture/product program needing a separate compatibility and migration design; it would obscure the registry acceptance boundary. |
| Feishu capability pluginization | #4993 | Moving every `lark-*` skill, workflow, dependency, configuration, and migration path is a release of its own. 0.5.3 must only ensure the Registry has the clean source model this plugin will consume. |
| Shared-resource coordination | #4991 | Cross-agent leases, recovery semantics, and CDP integration are a separate runtime foundation; they should not be hidden inside a patch release. |
| Channel expansion and channel-CLI abstraction | #4887, #4888, #4889 | A separate multi-channel product release; preserve Feishu regressions but do not add an adapter migration as a 0.5.3 gate. |
| Provider-neutral delegation and CLI redesign | #4973, #4982 | Both are cross-cutting proposals that require design decisions before implementation work is committed. |
| Security/quality audits and Secret Scanning permission | #4895, #4894, #4915 | Important ongoing work, but broad audits or an external App permission grant are not release gates. |
| Research, browser/CDP, daemon, and legacy deployment backlog | #4753, #4754, #4800, #4796, #4732, #4625, #4600, #4434, #4039 | Independent product or maintenance tracks with no P0 dependency. |

#4924 remains the open 0.5.1 release-tracking Issue. Resolve its tag/release evidence independently; do not relabel it as 0.5.3 feature work.

## Release gate

Before tagging, require build, type-check, lint, and focused regressions for every P0 acceptance criterion. Retain the installed-package matrix and release-candidate verification established by 0.5.1/0.5.2. The release is blocked by any cross-harness manifest divergence, trust-boundary bypass, false reconnect for a persistent error, dropped terminal delivery, or command schedule that cannot reach the current managed service endpoint.


## Review follow-up and current evidence (2026-09-13)

| Work | Current evidence | Remaining gate |
| --- | --- | --- |
| #4908 / PR #4995 | Shared `core/src/skills` module and public core exports; no harness dependency; 3 core tests pass | Re-review of the updated implementation and merge |
| #4909 / PR #4996 | Codex source adapter imports the public registry; shared manifest consumed by exec and app-server | Merge after #4995 |
| #4910 / PR #4997 | Trust checks now live in the shared module; 7 tests cover symlink escapes, metadata, unsafe identifiers, and rejection-cache refresh | Merge after #4996 |
| #4992 / PR #4998 | Explicit project root remains separate from the execution workspace | Merge after #4997 |
| #4989 / PR #4994 | Merged on main at `6ac98395` | Retain terminal-error regressions in the integrated candidate |
| #4984 / PR #4999 | Existing PR checks pass; child-process coverage verifies live endpoint/token inheritance | Real packaged CLI against a dynamically bound service, with and without authentication |
| #4907 | Not delivered | Track the actual final output message ID, including streaming and thread replies; exclude request/status messages, cancellation, and failed delivery; verify reaction retry behavior |

At stack head `d8a9b648`, build/type-check and lint pass. The shared registry,
Codex exec provider, and Codex app-server provider suites pass **75 tests**.
The unsafe-name and stale-diagnostic regressions were first reproduced as test
failures and then passed after the shared-core fixes.

The local integration commit `282d1ad1` combines this updated stack with main
`6ac98395` and PR #4999 head `a8fa616e`. Build/type-check and lint pass there;
the scheduler and ChatAgent suites pass a further **212 tests**, including the
managed command environment and persistent runtime-error paths. This is local
integration evidence, not a published candidate or a merge into main.

The release is **not ready to tag**. Open PRs and passing focused tests do not
replace the remaining completion-delivery and installed-package acceptance.
Do not mark cross-harness parity complete until the adapters in #4911/#4912
have their own integration evidence. See [the shared API contract](../skills-registry.md)
once #4995 merges.
