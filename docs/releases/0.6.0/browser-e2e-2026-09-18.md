# Browser natural-language E2E 2026-09-18

This is a repeatable browser-service result for the scope-clean candidate
`eafa0b2f8506a94711e8a2393d3231f350c7fe70`. It is separate from Feishu and
production-service acceptance.

Environment:

- macOS ARM64, Node 24.8.0, Codex CLI 0.154.0;
- Chromium at `/Applications/Chromium.app/Contents/MacOS/Chromium`;
- Anaconda Python `browser-use` 0.13.10 and `browser-harness` 0.1.13;
- `DISCLAUDE_E2E_BROWSER_CODEX=1` and `DISCLAUDE_E2E_BROWSER_NATURAL=1`.

The shipped `tests/e2e/browser-service.test.ts` ran the real product IPC entry
and the natural-language Codex task. The model discovered the shipped
browser-use skill, read the shared page, changed the input, created the PNG
artifact and passed independent readback. The run printed
`BROWSER_MODEL_HANDOFF` with `backend=codex`, `naturalTask=true`,
`previousStateVerified=true` and `independentReadback=true`.

Result: `1` test passed in `54.93s`. The same run printed
`BROWSER_SERVICE_CLEANUP` with `rootRemoved=true`, `callersClosed=true` and
`crashDescendantsGone=true`.

An earlier run of the same path reached the 90-second test deadline. That
transient failure remains in the local feedback log; this successful rerun is
the current candidate evidence. Neither run touched the daily Feishu service,
and this E2E does not prove live-site retrieval or real Feishu UI acceptance.
