/**
 * Agent configuration constants.
 */

/**
 * Agent configuration is now handled by the Agent SDK directly.
 * Tools are managed through SDK's built-in tool system.
 */
export const ALLOWED_TOOLS: string[] = [];
