/**
 * Shared utilities for Agent SDK integration - Re-exported from @disclaude/core
 *
 * @deprecated Import directly from '@disclaude/core' instead.
 */

export {
  getNodeBinDir,
  parseSDKMessage,
  extractText,
  buildSdkEnv,
} from '@disclaude/core';
