/**
 * Agent types - Re-exported from @disclaude/core for backward compatibility.
 *
 * @deprecated Import directly from '@disclaude/core' instead.
 */

export type {
  // Re-export extended types with their original names for backward compatibility
  ContentBlock,
  AgentMessageMetadata,
  ParsedSDKMessage,
  AgentMessage,
  AgentOptions,
  StreamingUserMessage,
  AgentInput,
  AgentMessageType,
} from '@disclaude/core';
