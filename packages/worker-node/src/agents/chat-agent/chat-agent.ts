/**
 * ChatAgent re-export.
 *
 * Issue #2717 Phase 1: Implementation migrated to @disclaude/core.
 * This file re-exports from core for backward compatibility.
 * Uses ChatAgentImpl (the class) from core, exported as ChatAgent.
 */

export { ChatAgentImpl as ChatAgent } from '@disclaude/core';
