{
  "ok": true,
  "platform": "darwin/arm64",
  "browser": "Chrome/155.0.8057.0",
  "checks": [
    "service launches Chromium on port 0 and discovers its endpoint",
    "real browser-use helpers share state across two queued clients; stale client cannot interfere",
    "cancelled allocation and invalid IPC method do not poison the next request",
    "caller disconnect cancels running CLI and harness; queued client successfully takes control",
    "owned harness daemon death releases control; next client creates a healthy existing-daemon session",
    "queued client cancellation does not block a later caller",
    "silent holder expires and waiting IPC client successfully operates",
    "stdin CLI uses socket only; heartbeat sustains a script beyond the lease TTL",
    "10 repeated real harness handoffs preserve one shared page"
  ],
  "persistence": {
    "required": false,
    "retained": false,
    "scope": "browser-restart",
    "storage": "system-default"
  },
  "browserUse": "0.13.10",
  "browserHarness": "0.1.13"
}
